Legacy files in this folder are kept only for reference while the app is being migrated to the modular screen structure under `js/screens/`.

Do not load these files in `index.html` or add new feature work here.
